# Functions in C++

Functions help break a program into manageable pieces.

```cpp
int add(int a, int b) {
    return a + b;
}

int main() {
    std::cout << add(3, 4);
    return 0;
}
```

## Key ideas

- Functions can take parameters.
- They can return a value.
- They make programs easier to maintain.
