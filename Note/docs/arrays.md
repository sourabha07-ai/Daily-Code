# Arrays in C++

Arrays store multiple values of the same type in contiguous memory.

```cpp
int nums[5] = {1, 2, 3, 4, 5};

for (int i = 0; i < 5; i++) {
    std::cout << nums[i] << " ";
}
```

## Notes

- Array size is fixed when declared.
- Elements are accessed using zero-based indexing.
